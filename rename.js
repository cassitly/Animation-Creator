// Set cover image file path (can be changed dynamically)
document.getElementById('cover-image').style.backgroundImage = "url('../assets/rs.jpg')";

// Set your song name (It will show below ur cover image)
document.getElementById('song-name').innerHTML = "Unbroken";

// Set your artist name (It will show below the song name in a smaller font)
document.getElementById('artist-name').innerHTML = "Casually Nyx";