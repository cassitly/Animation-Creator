// Set cover image file path (can be changed dynamically)
document.getElementById('cover-image').style.backgroundImage = "url('rs.jpg')";

// Set audio file path (can be changed dynamically)
document.getElementById('audio-player').src = "url('rs.wav')";

// Set your song name (It will show below ur cover image)
document.getElementById('song-name').innerHTML = "Default / Unset";

// Set your artist name (It will show below the song name in a smaller font)
document.getElementById('artist-name').innerHTML = "No Artist Name";